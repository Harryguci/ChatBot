name: chatbot-service
services:
  # PostgreSQL Database with pgvector extension
  postgres:
    image: pgvector/pgvector:pg15
    container_name: chatbot-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-chatbot_db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d:ro
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-chatbot_db}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - chatbot-network

  # Ollama LLM server (CPU-only by default)
  ollama:
    image: ollama/ollama:latest
    container_name: chatbot-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    networks:
      - chatbot-network

  # # Backend FastAPI service
  # backend:
  #   build:
  #     context: ..
  #     dockerfile: DockerFile
  #   container_name: chatbot-backend
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - PYTHONPATH=/app/src
  #     - ENVIRONMENT=production
  #     - DB_HOST=postgres
  #     - DB_PORT=5432
  #     - DB_NAME=${DB_NAME:-chatbot_db}
  #     - DB_USER=${DB_USER:-postgres}
  #     - DB_PASSWORD=${DB_PASSWORD:-postgres}
  #     - DB_POOL_SIZE=${DB_POOL_SIZE:-10}
  #     - DB_MAX_OVERFLOW=${DB_MAX_OVERFLOW:-20}
  #     # RAG Enhancement Settings
  #     - USE_FAISS=${USE_FAISS:-false}
  #     - CHUNK_SIZE=${CHUNK_SIZE:-1500}
  #     - CHUNK_OVERLAP=${CHUNK_OVERLAP:-200}
  #     - MULTI_QUERY_ENABLED=${MULTI_QUERY_ENABLED:-true}
  #     - OCR_ENABLED=${OCR_ENABLED:-true}
  #     - OCR_LANGUAGES=${OCR_LANGUAGES:-vie+eng}
  #     - TEXT_THRESHOLD=${TEXT_THRESHOLD:-150}
  #     - USE_LANGCHAIN_CHUNKING=${USE_LANGCHAIN_CHUNKING:-true}
  #     - TESSDATA_PREFIX=/usr/share/tesseract-ocr/4.00/tessdata
  #   volumes:
  #     - ../src:/app/src:ro
  #     - ../frontend/release:/app/frontend/release:ro
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8000/api/"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s
  #   networks:
  #     - chatbot-network

  # # Frontend React service (optional - for development)
  # frontend-dev:
  #   build:
  #     context: ../frontend
  #     dockerfile: DockerFile
  #   container_name: chatbot-frontend-dev
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - backend
  #   restart: unless-stopped
  #   networks:
  #     - chatbot-network
  #   profiles:
  #     - dev

  # # Nginx reverse proxy (optional - for production)
  # nginx:
  #   image: nginx:alpine
  #   container_name: chatbot-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - backend
  #   restart: unless-stopped
  #   networks:
  #     - chatbot-network
  #   profiles:
  #     - production

networks:
  chatbot-network:
    driver: bridge

volumes:
  chatbot-data:
    driver: local
  postgres_data:
    driver: local
  ollama_data:
    driver: local
